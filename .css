/* File: gemini.html (Style Section)
   Description: CSS definitions for Dark Mode and Layout.
*/

:root {
    --bg-color: #0f1012;
    --sidebar-color: #18191b;
    --surface-color: #242628;
    --input-bg: #1e1f21;
    --text-primary: #ededed;
    --rixi-gradient: linear-gradient(90deg, #b983ff, #94b3fd);
    --border-radius: 12px;
}

/* Global Reset */
* { margin: 0; padding: 0; box-sizing: border-box; font-family: 'Inter', sans-serif; }

body { 
    background-color: var(--bg-color); 
    color: var(--text-primary); 
    height: 100vh; 
    overflow: hidden; 
    display: flex; 
}

/* Authentication Card Styling */
.auth-card { 
    background: rgba(24, 25, 27, 0.8); 
    backdrop-filter: blur(12px); 
    border: 1px solid rgba(255,255,255,0.08); 
    padding: 2.5rem; 
    border-radius: 24px; 
    width: 100%; 
    max-width: 400px; 
    text-align: center; 
}

/* Chat Message Styling */
.message { 
    display: flex; 
    gap: 16px; 
    line-height: 1.6; 
    opacity: 0; 
    animation: fadeIn 0.5s forwards; 
    margin-bottom: 20px;
}

.message.user { flex-direction: row-reverse; }

.user .message-content { 
    background: #2e2f30; 
    padding: 10px 16px; 
    border-radius: 18px 4px 18px 18px; 
}

/* Typing Indicator Animation */
.typing-indicator span { 
    display: inline-block; 
    width: 6px; 
    height: 6px; 
    background-color: var(--text-secondary); 
    border-radius: 50%; 
    animation: typing 1.4s infinite both; 
    margin: 0 2px; 
}

@keyframes typing { 
    0%, 80%, 100% { transform: scale(0); } 
    40% { transform: scale(1); } 
}
